# Disable Git submodules completely
[git]
  submodules = false

# Build configuration
[build]
  base = "Glamora-App-main"
  publish = "admin-side"
  command = "echo 'Deploying static admin-side files'"

# Node.js version
[build.environment]
  NODE_VERSION = "22"
