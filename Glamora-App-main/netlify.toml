[build]
  base = ""
  publish = "admin-side"
  command = ""
  skipProcessing = false

# Ignore git submodules
[build.environment]
  NPM_FLAGS = "--ignore-scripts"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

